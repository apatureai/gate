# Apature Gate - Build Progress (loop source of truth)

This file is the deterministic checklist for the autonomous build loop. Work top-down.

How the loop uses it:
- Pick the FIRST unchecked `[ ]` issue whose dependencies are already `[x]`.
- When done (code + tests green, pushed): change `[ ]` to `[x]` and append ` -> done: <one line>`.
- If blocked, mark `[~]` and append ` -> skipped: <reason>`, then take the next unblocked issue.
- Build order is M0 -> M1 -> M2 -> M3 -> M4. Cross-repo deps tagged [judgment-engine #N] are MOCKED, not built here.


## M0 · Foundation & runtime

- [x] #30 - Infra: service + Action monorepo scaffold + shared types package -> done: pnpm workspace (types/service/action) + tsc -b project refs; @gate/types exports the engine boundary contracts (§6 + §15.2 amendments) and the shared golden fixture loader; service/action consume them; no contents:write; typecheck+test(12)+lint green
- [x] #31 - Infra: lint + typecheck + test CI pipeline -> done: .github/workflows/ci.yml runs pnpm install --frozen-lockfile + lint + tsc -b + tests on every PR/push; lint is warnings-as-errors (eslint --max-warnings=0); typecheck covers all packages via project refs. NOTE: marking the check "required before merge" is a one-time branch-protection setting for a maintainer (needs admin; left for human).
- [x] #33 - Infra: Postgres provisioning + migrations (installations, runs, feedback_events, billing) -> done: packages/db with idempotent migration runner (schema_migrations tracking) + 0001_init.sql (installations, runs w/ UNIQUE(pr,head_sha)+last_full_review_at+expires_at, feedback_events, billing_customers, webhook_log); pg adapter for deploy + PGlite-backed tests in CI; migrate CLI for Fly release cmd (wired in #32). NOTE: real Neon/Fly provisioning is an ops step.
- [x] #34 - Infra: Redis provisioning (BullMQ + current_sha + per-installation token-bucket) -> done: packages/redis with resilient ioredis client (maxRetriesPerRequest:null + capped retryStrategy, no silent drops), bull:/sha:/tb:/cb: namespaces + key builders (supersessionKey, tokenBucketKey, cb:engine), and assertNoEviction() guard enforcing noeviction so sha: keys are never dropped. NOTE: real Upstash/Fly Redis provisioning is an ops step.
- [x] #35 - Infra: secrets + KMS envelope for gate-held secrets -> done: packages/secrets with SecretStore (app secrets incl. engineEndpoint) + EnvSecretStore dev seam; AES-256-GCM envelope (sealSecret/openSecret) wrapping a per-repo DEK under a CMK (shared/per-tenant, never per-repo KMS keys), GCM integrity-checked; redact() scrubs secrets/signed-URLs/storageState/screenshot data from logs; storageStateForPr/assertStorageStateAllowed disable storageState on fork PRs. NOTE: bind SecretStore/KmsKeyProvider to real AWS KMS in ops. [hardening: redact() now guards self-referential ARRAYS (not just objects) so a circular array in a log payload can't stack-overflow the scrubber.]
- [x] #50 - Security: Postgres RLS tenant isolation + isolation test suite -> done: 0002_rls.sql ENABLE+FORCE RLS with installation_id-scoped USING/WITH CHECK policies on installations/runs/feedback_events/billing_customers (default-deny when GUC unset); withTenant() sets app.current_installation_id per transaction via set_config; isolation suite (run under a non-superuser role since pglite superuser bypasses RLS) proves cross-tenant read/update/insert are denied + default-deny. CI runs it.
- [x] #32 - Infra: Fly.io app + deploy for the App/orchestrator service -> done: minimal Fastify server (buildServer) with /healthz (#1 extends it); Dockerfile (multi-stage, prune --prod, no secrets in image) + .dockerignore; fly.toml deploys to Fly Machines with a /healthz http check, release_command running db migrations, min 1 machine; .github/workflows/deploy.yml runs flyctl deploy on merge to main (FLY_API_TOKEN). NOTE: creating the Fly app + setting fly secrets are ops steps.
- [x] #36 - Infra: OpenTelemetry traces + metrics + dashboards/alerts (stale-publish=0) -> done: packages/observability with SPAN_NAMES taxonomy (webhook->preview->enqueue->engine->guard->publish) + withSpan helper; GateMetrics (stale_publish counter [must be 0], review/time-to-first-comment latency, queue_depth gauge, engine errors/submit-latency/poll-count/cancels/timeouts, capture instability); initTelemetry wires OTel providers (exporters injected/OTEL_* env). observability/alerts.yaml fires on stale_publish>0 + SLO alerts; observability/dashboard.json panels for latency/queue depth/engine errors. Tested via OTel in-memory exporters.

## M1 · Action path

- [x] #27 - Config: .designreview.yml schema + validation + defaults -> done: packages/config with a Zod schema covering preview/routes/viewports/dark_mode/brand/rules/tokens (.strict() catches typos); every field has a working default so the file is optional; loadDesignReviewConfig parses YAML + normalizes snake_case -> NormalizedDesignReviewConfig (added tokens to @gate/types); ConfigValidationError surfaces readable issues (incl. YAML syntax). Shared schema export for extractor + dashboard.
- [x] #8 - Orchestrator: preview-URL discovery - Action path (explicit + url_template + provider-bot + local-serve) -> done: resolvePreviewUrl() in @gate/action resolves explicit -> url_template ({pr}/{sha}/{short_sha}) -> allowlisted provider-bot comment (gated on config.preview.source naming a known provider + bot-login allowlist + domain-suffix match, so free-text comments are never trusted) -> local-serve (preview-command); returns url + source + provider + provenance, or a reason listing attempts.
- [x] #45 - Engine: async job API contract (POST/GET/DELETE /jobs, idempotency, poll/cancel) -> done: packages/engine job protocol — idempotencyKey(pr:headSha), depth-aware backoff (triage 10s+/deep 30s+, then +10s) capped at the 10-min REVIEW_DEADLINE_MS; runEngineJob submits then polls (409 polls existing job, no re-capture); timeout returns review_timed_out with no retry; cancelEngineJob best-effort; fetch-based HTTP transport behind a seam (HMAC #47 / zod parse #46 layer on later). NOTE: #45's stated dep on #37 is inverted — the contract precedes the client; #37 will build on this.
- [x] #37 - Engine: judgment-engine client (GateReviewRequest -> GateReviewResult) -> done: createJudgmentEngineClient in @gate/engine — buildGateReviewRequest assembles the request from preview+config+PR context; review() submits via the #45 job protocol (idempotent per pr:headSha) with bounded submit retry + backoff and polls to a GateReviewResult; extractReviewMetadata surfaces engineVersion/model/promptVersion/captureVersion/uiDnaVersion (model is engine-selected, never Claude). Auth+timeout in the transport; #46 (zod parse) / #47 (HMAC) layer on next.
- [x] #47 - Auth: HMAC-signed Gate->engine requests scoped to installationId -> done: signEngineRequest/verifyEngineRequest (HMAC-SHA256 over `${ts}.${installationId}.${body}`, constant-time compare, optional skew window) in @gate/engine; HTTP transport signs the submit body when hmacSecret set (installationId bound in headers) so the engine verifies + scopes storage to the verified tenant; secret sourced from @gate/secrets engineHmacSecret (KMS). Verifier returns typed reasons for caller logging/alert. Engine-side verify+alert is cross-repo (mocked).
- [x] #39 - Security: preview-URL source verification before engine handoff -> done: verifyPreviewHandoff in @gate/engine independently re-checks provenance at the boundary — forwards only verified origins (deployment_status/explicit/url_template/provider-bot/local), enforces per-provider domain match (loopback for local, any http(s) for explicit), returns not_reviewed: unverified_preview_source for free-text/mismatched URLs, and disables bypass+storageState secrets on fork PRs (via @gate/secrets). Engine still owns SSRF/DNS-rebind.
- [x] #46 - Contract: x-schema-version header + Zod runtime parse + golden fixture -> done: GateReviewResultSchema + parseEngineResult in @gate/engine validate the x-schema-version major then Zod-parse (version mismatch degrades gracefully; shape mismatch is a typed error that blocks publish, never null-grade); HTTP transport enforces it on completed jobs; schema is additive-tolerant (strips unknown fields). Golden fixture already shared by mock+engine; @gate/types README documents the additive-only evolution rule. uiDnaVersion (string|null) covered.
- [x] #38 - Engine: failure & degradation handling -> done: decideDelivery in @gate/delivery maps every engine outcome to safe non-blocking delivery — timeout/failed/unavailable/circuit-open -> neutral Check Run + retry guidance (PR never failed); completed -> validateFindings drops invalid element refs (caveat), capture-unstable adds a confidence caveat, notReviewed always rendered; composes with the publish-time SHA guard. Plus parseRetryAfterMs + RetryableEngineError so the client honors Retry-After on 429/503. (malformed result already blocked by #46 zod parse.) [hardening: runAction (Action) + runHostedReview (App) now catch a thrown engine error (network/5xx/contract violation) and publish a neutral Check Run via decideDeliveryForError instead of escaping -- the Action path previously posted no Check Run at all on engine failure, and the App worker would have crashed. decideDeliveryForError was implemented+tested in #38 but wasn't wired into the orchestrators.]
- [x] #10 - CI: sticky PR comment (hidden marker + optimistic node_id) -> done: packages/delivery renderStickyComment (hidden marker, grade + reviewed SHA, blockers table, collapsed should-fix/nits details, always-rendered Not reviewed section, page-health footnote with engine/model/ui-dna lineage + run link, optional capture caveat) + upsertStickyComment (find-by-marker, one comment/PR, optimistic node_id check -> skipped_stale when a newer writer won). GitHub client injected (identical across Action GITHUB_TOKEN / App token).
- [x] #11 - CI: Check Run conclusion mapping (default non-blocking) -> done: mapCheckRunConclusion + buildCheckRun in @gate/delivery — ship/ship_with_nits->success, needs_work->neutral, blocked->failure only when gate:blockers, never-blocking by default; Check Run carries name+grade title+summary+details link (TRD §7). Consolidated the #30 scaffold helper from @gate/service (now re-exports from @gate/delivery).
- [x] #12 - CI: annotated screenshots (sharp from geometry) + stable /i/<id>.png -> 302 signed URL -> done: @gate/delivery annotateScreenshot composites SVG boxes via sharp from recorded geometry rects (clamped to bounds, severity colors), never VLM pixel coords; @gate/service registerScreenshotRoute serves the permanent /i/:id.png route that 302s to a freshly-signed URL and 410-tombstones after expiresAt (receivedAt+screenshotRetentionSeconds); stableScreenshotUrl/buildRunUrl/buildScreenshotRecords keep comments on stable routes (no raw expiring links). Bucket/signing engine-owned (mocked).
- [x] #22 - Action: GitHub Action entrypoint (action.yml + Docker, runner capture) -> done: action.yml (Docker action, inputs preview-url/preview-command/config-path/gate-mode/github-token, no contents:write) + Dockerfile.action; runAction orchestrates resolve preview (#8) -> verify handoff (#39) -> submit async engine job (#37/#45) -> decideDelivery (#38) -> sticky comment (#10) + Check Run (#11) via the Action GITHUB_TOKEN; thin fetch-based GitHub REST adapter (createGitHubApi) + main.ts entry. Capture/annotation stay engine-side; action exits 0 (Check Run is the gate, PR never failed by default).
- [x] #40 - Test: end-to-end acceptance harness (TRD §11) -> done: packages/e2e drives the real runAction + HTTP transport + contract parse against a MOCK engine (fake fetch serving the golden fixture w/ x-schema-version) and an in-memory GitHub; asserts explicit-URL PR produces an annotated review (sticky comment w/ findings+lineage + Check Run), a blocker in advisory mode stays neutral (blocks only under gate:blockers), and the flow needs no contents:write. Runs in CI via vitest; no live model calls.
- [x] #51 - Security: Action-path hostile-PR capture threat model -> done: docs/threat-model-action-path.md (what the capture browser can reach in the customer runner; why the App path differs via engine Firecracker sandbox; Gate-owned provenance+fork-gating vs engine-owned SSRF/egress) + packages/action/README warns against pull_request_target with secrets; guard test asserts the doc sections + re-affirms in code that fork PRs disable storageState/bypass and the Action never requests contents:write.

## M2 · App path

- [x] #1 - Orchestrator: Fastify server + webhook receiver -> done: buildServer adds POST /webhook accepting pull_request + deployment_status (dispatches to injected handlers, accept-and-ignores other events with 2xx so GitHub stops retrying), /healthz + /readyz (readiness probe), and a per-request OTel span hook (webhookReceive for /webhook, http <method> else) with status/route attributes. HMAC verify is #2. Tested incl. span emission via InMemorySpanExporter.
- [x] #2 - Orchestrator: GitHub App auth + HMAC webhook verify -> done: createGitHubAppAuth (@octokit/app) mints app JWTs + caches installation tokens; createWebhookVerifier (@octokit/webhooks) verifies x-hub-signature-256; buildServer captures the raw JSON body and rejects forged/unsigned /webhook deliveries with 401 before any work. Secrets from @gate/secrets (privateKey/webhookSecret). Tested: good/bad/wrong-secret/missing signature + offline JWT minting.
- [x] #55 - Orchestrator: deployment_status preview discovery (webhook -> SHA match -> dedupe) -> done: resolveDeploymentPreview in @gate/service filters state==success + configured environment (default Preview, ignores storybook/non-app), extracts environment_url/target_url, matches deployment.sha to expectedHeadSha, and dedupes on (sha, deployment_id) via injected isDuplicate so a redeploy of the same SHA doesn't re-trigger; returns url+sha+deploymentId+dedupeKey+source. Feeds verifyPreviewHandoff like the Action path.
- [x] #3 - Orchestrator: BullMQ queue (key repo#pr, completed-id pr+head_sha) -> done: @gate/service queue — reviewQueueKey(repo#pr) is the jobId/supersession key (createReviewQueue enqueues remove-then-add so the newest push wins), completedReviewId(pr:head_sha) is the completed-review identity, ReviewJobPayload carries IDs/refs only (artifacts stay in engine object storage); createBullReviewQueue wraps BullMQ Queue behind the ReviewQueue interface (Inngest-swappable, #48) on a resilient @gate/redis connection under bull:. ioredis deduped via pnpm override.
- [x] #48 - Orchestrator: ReviewJobWorker interface + BullMQ adapter (Inngest migration path) -> done: ReviewJobWorker interface (enqueue/cancel/onJob) in @gate/service; CancellationRegistry (one AbortController per repo#pr, newest-wins) drives cooperative cancel; createInMemoryReviewWorker is a tested reference impl (supersession + signal abort), createBullReviewWorker is the BullMQ adapter (Worker threads the signal into the job ctx). docs/queue-migration-inngest.md documents the Inngest singleton-cancel path + trigger (stale-publish non-zero 2 wks); publish-time SHA guard stays queue-agnostic.
- [x] #4 - Orchestrator: supersession (current_sha + AbortController + publish-time guard) -> done: @gate/service supersession — recordEnqueue sets current_sha[repo#pr] (sha: key), isCurrentSha is the stage-boundary check, guardPublish re-reads current_sha and discards stale (queue-agnostic backstop, holds even if AbortSignal bypassed); engine pollUntilDone now honors a supersession AbortSignal at stage boundaries (throws EngineAbortedError); comment edit optimistic node_id via #10. Tested: two-jobs-only-newest-publishes + guard-discards-stale + signal abort. [hardening: AbortSignal now threaded into the engine HTTP client (transport.poll/submit accept it, combined with the per-request timeout via AbortSignal.any), and a supersession-aborted in-flight fetch is converted to EngineAbortedError -- closes the §15.3 mandatory checkpoint that the signal reach the client, not just the poll loop.]
- [x] #5 - Orchestrator: readiness-driven debounce (120s ceiling) + wait_seconds floor -> done: waitForReadiness in @gate/service polls the preview for HTTP 200 up to a 120s ceiling (READINESS_CEILING_MS), honors wait_seconds as an override floor, and has no fixed 90s timer; abort-and-restart on supersession via AbortSignal (returns aborted, caller restarts for the new SHA); in-page stability/first-paint handed off to the engine. Tested with fake fetch + virtual clock + abort.
- [x] #6 - Orchestrator: concurrency (repo,pr)=1 + per-installation fair scheduling -> done: @gate/service scheduling — (repo,pr)=1 enforced by the queue jobId + selectNextJobs skipping in-flight/duplicate keys; InstallationConcurrency caps per-installation in-flight against a tier cap (TIER_CONCURRENCY free1/team3/business5/enterprise10) via the tb: counter (acquire incr-then-rollback); selectNextJobs round-robins one job per installation per pass so a hot PR/installation can't starve a busy monorepo. Inngest account-concurrency is the migration path. Tested fairness/cap/(repo,pr)=1.
- [x] #7 - Orchestrator: 10-min full-review cap -> triage-only -> done: @gate/service review-window — chooseReviewDepth returns triage if a full review ran within FULL_REVIEW_WINDOW_MS (10 min) else deep; decideDepthForPush reads the per-PR window; createInMemoryFullReviewWindow (tests) + createSqlFullReviewWindow read/write the durable runs.last_full_review_at (Postgres, so a Fly restart can't reset the cap). Tested: window boundary, first-deep/re-push-triage/after-deep, per-PR independence.
- [x] #43 - Engine: review-depth signaling (triage vs deep) + 10-min-cap coordination -> done: @gate/service depth-policy — decideReviewDepth chooses triage/deep from the 10-min full-review window (#7) with a typed reason (no_prior_full_review/within_full_review_window/full_review_window_elapsed); recordFullReviewIfDeep stamps the window only for deep; traceDepthDecision emits a gate.depth.decision span (depth+reason attrs) for tuning. depth rides the POST /jobs body (#45) with depth-aware backoff; composes with concurrency (#6) + supersession (#4).
- [x] #49 - Orchestrator: webhook delivery dedup + GitHub secondary-rate-limit backoff -> done: WebhookDedupeStore (in-memory + SQL ON CONFLICT on webhook_log delivery_id PK, pglite-tested) wired into /webhook (duplicate X-GitHub-Delivery -> 200 skip, no double dispatch); withRateLimitRetry/isRateLimited/rateLimitDelayMs honor Retry-After (secondary) + x-ratelimit-remaining/reset (primary) with exp backoff + jitter, capped. runs UNIQUE(pr,head_sha) already in #33; /i/<id>.png 410 tombstone already in #12. [hardening: moved the rate-limit helper to the shared @gate/engine layer and wired it into the concrete Action GitHub client (createGitHubApi) so every GitHub call (list/create/update comment, check run) actually honors primary+secondary limits with backoff; tested via a 429-then-success retry.]
- [x] #9 - Orchestrator: Vercel Protection bypass + multi-deployment filtering -> done: vercelBypassHeaders(secret) emits x-vercel-protection-bypass (+ set-bypass-cookie) from the stored VERCEL_AUTOMATION_BYPASS_SECRET; waitForReadiness now accepts headers so the probe gets 200 through protection; resolveDeploymentPreview gained allowedEnvironments (Preview + allowlist, still ignores storybook); filterAppDeployments narrows a PR's many deployments to the app preview, deduping redeploys on (sha, deployment_id) across the batch. Secret never logged.
- [x] #21 - Security: minimal GitHub App permissions (never contents:write) -> done: GATE_APP_PERMISSIONS = exactly {checks:write, pull_requests:write, contents:read, deployments:read} (4 scopes, never contents:write); buildAppManifest produces the create-from-manifest manifest (default_permissions = the 4 + default_events pull_request/deployment_status + neutrality-guarantee description); assertNoContentsWrite guard. Rationale documented in code + manifest description. Tested.
- [x] #13 - CI: feedback signal endpoints — POST-only (reaction API / slash command + HMAC) -> done: @gate/service feedback — mintFeedbackToken/verifyFeedbackToken (HMAC, expiry, jti) + single-use ConsumedTokenStore; registerFeedbackRoutes: GET /feedback/confirm renders an inert noindex POST form, GET /feedback -> 405, POST /feedback verifies the one-time token then records via injected FeedbackSink (urlencoded + json); parseDesignReviewCommand parses /design-review. GET prefetch mutates nothing. Feedback store/forward is #41.
- [x] #41 - Data: feedback event model + forward to shared store -> done: @gate/service feedback-store — buildFeedbackEvent covers all event types (finding_posted/reaction/slash_command/ignore_suppress/merged_with_unresolved_blockers/suggestion_adopted) stamping source + rater permission; createSqlFeedbackStore inserts into feedback_events (pglite-tested on real schema), createHttpFeedbackForwarder forwards to the shared engine store; createFeedbackSink persists then best-effort forwards (failure never drops the record); detectSuggestionAdoption is string-match on suggested tokens only (touching the element doesn't count). Non-collaborator down-weighting stays engine-owned.
- [x] #23 - App: hosted GitHub App install path -> done: @gate/service hosted-review — runHostedReview composes the App path (verify preview #39 -> depth #43 -> engine job with supersession signal #4/#45 -> publish-time guard #4 -> delivery #38 -> sticky comment #10 + Check Run #11 -> feedback #41); createDeploymentStatusHandler wires deployment_status (#55) -> record current_sha -> enqueue (#3/#48); manifest uses minimal perms (#21, never contents:write). Tested: published/stale-discarded/superseded/unverified + handler resolve+enqueue. Hosted-tier storage wiring lands with the dashboard (M3). [hardening: createDeploymentStatusHandler now scopes installationId from the webhook installation.id (was hardcoded ""); added hydrateReviewContext + PullRequestFetcher so the worker turns the IDs-only payload into a review context by fetching PR title/body/isFork; added an App-path end-to-end integration test (deployment_status -> worker -> hydrate -> runHostedReview -> publish).] [hardening: createDeploymentStatusHandler now derives repo (repository.owner.login/name) + installation from the webhook payload instead of a fixed repository arg, so one handler serves every installed repo (multi-tenant correctness); resolvePullRequest is (owner,name,sha). Added createAppWebhookHandlers wiring #1's webhook receiver to the orchestration: pull_request push bumps current_sha + cancels the in-flight review (newest wins #4), deployment_status resolves+enqueues.]
- [x] #28 - Config: npx designreview auth storageState wizard -> done: @gate/secrets storage-state — originScopeStorageState keeps only cookies/localStorage for the allowed origins (cookie .domain scoping handled), sealStorageState/openStorageState envelope-encrypt under the tenant CMK, prepareStorageStateArtifact scopes-then-seals the per-repo artifact; designreview CLI (bin) reads a recorded storageState, scopes + seals, writes the sealed file. Browser recording is the capture repo (#25); storageState disabled on fork PRs at use (#35). Tested.
- [x] #52 - Data: tenant offboarding + crypto-shredding pipeline -> done: offboardTenant (@gate/service) deletes the tenant rows (createSqlTenantDeleter: feedback_events/runs/billing/installations, pglite-tested), and for paid/enterprise destroys the per-tenant CMK (InMemoryTenantKms.destroyTenantKey in @gate/secrets) so artifacts encrypted under it are crypto-shredded (openSecret then throws); free tier = row deletion only (shared CMK); audited with a timestamp; expireArtifacts hook 410s the stable routes. Triggered by installation.deleted/dashboard. Per-tenant-CMK-everywhere is the M3 enterprise lever.

## M3 · Hosted tier

- [x] #15 - Dashboard: Next.js shell + auth -> done: @gate/dashboard core (tested, fits the tsc-b/vitest harness) — GitHub OAuth flow (buildAuthorizeUrl/generateOAuthState/exchangeCodeForToken/fetchUserInstallations), signed sessions (mintSession/verifySession), installation-scoped access (canAccessInstallation/filterAccessibleInstallations/assertInstallationAccess, defense-in-depth over RLS #50), and the NAV_ITEMS nav-shell model. The Next.js rendering shell is a thin consumer of this core (UI rendering kept out of the unit harness; documented).
- [x] #16 - Dashboard: run history + finding browser -> done: @gate/dashboard runs — listRunHistory queries per-repo runs newest-first (tenant-scoped/RLS, pglite-tested); buildFindingBrowser turns an engine result into a finding browser with each finding's stable /i/<id>.png screenshot URL (#12) and a prUrl linking the run back to its PR. Findings/screenshots stay engine-owned artifacts referenced by stable route; UI renders the view models.
- [x] #17 - Dashboard: feedback stats -> done: @gate/dashboard stats — classifyFeedback maps events to positive/negative/neutral; computeFeedbackStats gives acceptance rate over decisive feedback; statsByDimension breaks rates down by a dimension (e.g. severity); feedbackTrend buckets acceptance by day; loadFeedbackEvents sources from feedback_events (tenant-scoped/RLS, pglite-tested). UI renders the aggregates.
- [x] #18 - Dashboard: config UI (.designreview.yml editor + validate) -> done: @gate/dashboard config-ui — validateConfig runs edits through the @gate/config schema (#27) surfacing readable issues (incl. YAML syntax); copyableConfig gives the paste-able output; buildProposeConfigUrl deep-links into GitHub's own new-file editor (prefilled filename+value) so the user opens the PR themselves. No contents:write path exists — Gate writes nothing.
- [x] #19 - Billing: Stripe subscriptions + free-tier limits -> done: @gate/dashboard billing — PRICE_PER_SEAT_CENTS=$20/dev/mo + computeMonthlyTotalCents; verifyStripeSignature (t/v1 HMAC + tolerance), mapStripeEvent -> billing state, createSqlBillingStore upserts billing_customers (pglite-tested); free-tier gating: canReviewRepo (public-repos only) + freeTierDepth (triage + one deep per PR); usage = runs (countDeepReviewsForPr/countReviewsForRepo). Composes with depth policy #43. [hardening: verifyStripeSignature accepts ANY matching v1 in the Stripe-Signature header (multiple v1 appear during webhook-secret rotation) instead of only the last.]
- [x] #20 - Billing: enterprise SSO + in-VPC residency option -> done: @gate/dashboard enterprise — buildOidcAuthorizeUrl (OIDC SSO) + requiresSso/assertEnterpriseSso (SSO required for enterprise; SAML config modeled); canUseInVpcEngine/assertInVpcAllowed gate the in-VPC engineEndpoint to enterprise only (standard/paid = fully-managed hosted, no key mgmt); sealEngineEndpoint/resolveEngineEndpoint KMS-encrypt the per-account endpoint (feeds #53 createAccountEngineTransport, no silent fallback). No BYOK. Tested.
- [x] #29 - Config: onboarding flow (brand block, protection_bypass, preview source) -> done: @gate/dashboard onboarding — ONBOARDING_STEPS asks the brand block first; PROVIDER_GUIDES documents Vercel + Netlify (Deploy Preview) + Cloudflare (pages.dev) + Render equivalents; buildOnboardingConfig emits a validated .designreview.yml (brand + preview source/env + protection_bypass name, default env per provider); sealProtectionBypass KMS-stores the bypass secret value. Config adopted by the user (no contents:write, #18).
- [x] #53 - Enterprise: in-VPC judgment-engine endpoint (per-account engineEndpoint, no-fallback) -> done: @gate/engine endpoint-routing — resolveEngineRoute defaults to hosted, routes to a per-account in-VPC engineEndpoint (Gate-internal routing, KMS-decrypted by caller, not a GateReviewRequest field); createAccountEngineTransport targets exactly one base URL with NO fallback path to hosted, so an in-VPC outage errors (-> not_reviewed via #38) and never leaks screenshots to hosted; docs/in-vpc-engine.md describes the mode + residency guarantee. (Built before #20 to break the circular dep.)
- [x] #54 - Hardening (deferred, M3): webhook callback / Pact / JWT / Inngest / outbox -> done: docs/deferred-hardening.md tracks the 5 scale-tier migrations (D1 completion-webhook, D4 Pact, D4 JWT/JWKS, D2 Inngest singleton-cancel, D5 outbox+reconciliation) each with current MVP state + named trigger, and the hard invariants that survive every migration (no contents:write, supersession/completed-review identity, publish-time guard, stale-publish=0). Guard test asserts coverage. Split into issues when a trigger fires.

## M4 · Trust polish

- [x] #14 - CI: baseline comparison v1.5 (before/after pairs) -> done: @gate/delivery baseline — BaselineStore keeps the main-branch capture set per (route,viewport); buildComparisonPairs pairs each PR capture with its baseline (before null for new routes) so the deep pass gets before/after side by side; buildBeforeAfterArtifact emits the before/after pair with stable /i/<id>.png URLs. Screenshots stay engine-owned; Gate stores refs + pairs.
- [x] #24 - GTM: GitHub Marketplace listing + verification (start week 1) -> done: docs/marketplace-listing.md (listing copy, categories, events, $20/dev/mo pricing #19, minimal-permissions-never-contents:write lead #21, assets + verification checklist; not gated on the engine quality gate). Guard test asserts the listing matches GATE_APP_PERMISSIONS. Publisher verification + recording assets are human/marketing actions.
- [x] #25 - GTM: launch demo — agent-breaks-the-design-system caught in 90s -> done: docs/launch-demo.md runbook (scripted design-system break options + expected ~90s annotated catch + the fix flips green + distribution plan). Recording/posting the video is the human/marketing step.
- [x] #26 - GTM: public-judgment-on-OSS content engine -> done: docs/oss-content.md consent + opt-out process (explicit maintainer opt-in recorded; publish annotated artifacts w/ lineage, never secrets; opt-out stops runs + unpublishes via 410/#12 + offboard/#52, honored in 1 business day). Running/posting is human-driven.
- [x] #42 - Test/GTM: YC demo golden-path repo + automated <90s review check -> done: docs/golden-path-demo.md spec + @gate/e2e golden-path.test.ts smoke test against the mock engine (break PR -> annotated screenshot-grounded review within the 90s budget; fix re-run flips the Check Run to success). Scheduled live-pipeline run is an ops wiring step.
