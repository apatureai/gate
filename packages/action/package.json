{
  "name": "@gate/action",
  "version": "0.1.0",
  "private": true,
  "description": "Gate Action path: GitHub Action entrypoint that runs inside the customer runner.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@gate/config": "workspace:*",
    "@gate/delivery": "workspace:*",
    "@gate/engine": "workspace:*",
    "@gate/secrets": "workspace:*",
    "@gate/types": "workspace:*",
    "sharp": "^0.35.3"
  }
}
