{
  "name": "@gate/secrets",
  "version": "0.0.0",
  "private": true,
  "description": "Gate-held secret custody: KMS-backed store, envelope encryption, log redaction, fork-PR guards.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "designreview": "./dist/cli/auth.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "fast-check": "^4.9.0"
  }
}
