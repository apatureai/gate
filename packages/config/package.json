{
  "name": "@gate/config",
  "version": "0.0.0",
  "private": true,
  "description": "Schema, validation, and defaults for .designreview.yml. Shared by extractor and dashboard.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@gate/types": "workspace:*",
    "yaml": "^2.6.1",
    "zod": "^3.23.8"
  }
}
